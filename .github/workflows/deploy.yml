name: Deploy to Production

on:
  push:
    branches: [main]
    paths:
      - 'server/**'
      - 'client/**'
      - 'k8s/**'
      - 'aws/**'

jobs:
  # ─────────────────────────────────────────────────────────────────────────────
  # Stage 1 — Detect which paths changed
  # ─────────────────────────────────────────────────────────────────────────────
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      server: ${{ steps.filter.outputs.server }}
      client: ${{ steps.filter.outputs.client }}
      k8s: ${{ steps.filter.outputs.k8s }}
      aws: ${{ steps.filter.outputs.aws }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check for file changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            server:
              - 'server/**'
            client:
              - 'client/**'
            k8s:
              - 'k8s/**'
            aws:
              - 'aws/**'

  # ─────────────────────────────────────────────────────────────────────────────
  # Stage 2 — Build (client-build runs only for client changes)
  # ─────────────────────────────────────────────────────────────────────────────
  client-build:
    name: Client — Build
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.client == 'true'
    env:
      NEXT_PUBLIC_CASHFREE_ENV: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install dependencies
        run: cd client && npm ci

      - name: Build client
        run: cd client && npm run build

  # ─────────────────────────────────────────────────────────────────────────────
  # Stage 3 — Tests (client-test and server-test run in PARALLEL)
  # ─────────────────────────────────────────────────────────────────────────────
  client-test:
    name: Client — Test
    runs-on: ubuntu-latest
    needs: [changes, client-build]
    if: needs.client-build.result == 'success'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install dependencies
        run: cd client && npm ci

      - name: Run tests
        run: cd client && npm test

      - name: Annotate summary
        run: |
          echo "## Client — Build & Test" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check  | Status  |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|---------|" >> $GITHUB_STEP_SUMMARY
          echo "| Build  | passed |" >> $GITHUB_STEP_SUMMARY
          echo "| Tests  | passed |" >> $GITHUB_STEP_SUMMARY

  server-test:
    name: Server — Test
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.server == 'true'
    env:
      JWT_SECRET: test-jwt-secret-for-ci-do-not-use-in-production
      ENCRYPTION_KEY: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
      NODE_ENV: test
      PORT: "0"
      CLIENT_URL: http://localhost:3000
      CORS_ORIGIN: http://localhost:3000
      # MONGODB_URI is provided at runtime by mongodb-memory-server in globalSetup
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json

      - name: Install dependencies
        run: cd server && npm ci

      - name: Run tests
        run: cd server && npm test

      - name: Annotate summary
        run: |
          echo "## Server — Build & Test" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check  | Status  |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|---------|" >> $GITHUB_STEP_SUMMARY
          echo "| Build  | passed |" >> $GITHUB_STEP_SUMMARY
          echo "| Tests  | passed |" >> $GITHUB_STEP_SUMMARY

  # ─────────────────────────────────────────────────────────────────────────────
  # Stage 4 — Build Docker image and push to GHCR
  # ─────────────────────────────────────────────────────────────────────────────
  build-push-server:
    name: Build & Push Docker Image
    runs-on: ubuntu-24.04-arm
    needs: [changes, server-test]
    if: |
      needs.changes.outputs.server == 'true' &&
      needs.server-test.result == 'success'
    permissions:
      packages: write
      contents: read
    outputs:
      image: ${{ steps.image-tag.outputs.image }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get server version
        id: version
        run: echo "version=$(node -p "require('./server/package.json').version")" >> $GITHUB_OUTPUT

      - name: Lowercase owner
        id: owner
        run: echo "name=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ steps.owner.outputs.name }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tag output
        id: image-tag
        run: echo "image=ghcr.io/${{ steps.owner.outputs.name }}/edgebalancer-server:${{ steps.version.outputs.version }}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./server
          push: true
          platforms: linux/arm64
          provenance: false
          sbom: false
          tags: |
            ghcr.io/${{ steps.owner.outputs.name }}/edgebalancer-server:${{ steps.version.outputs.version }}
            ghcr.io/${{ steps.owner.outputs.name }}/edgebalancer-server:${{ github.sha }}
            ghcr.io/${{ steps.owner.outputs.name }}/edgebalancer-server:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Delete old GHCR versions (keep current + 2 previous)
        uses: actions/delete-package-versions@v5
        with:
          package-name: edgebalancer-server
          package-type: container
          min-versions-to-keep: 3
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Annotate summary
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          echo "## Docker Image — Built & Pushed to GHCR" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Detail   | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Version  | \`v$VERSION\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Image    | \`ghcr.io/${{ steps.owner.outputs.name }}/edgebalancer-server:$VERSION\` |" >> $GITHUB_STEP_SUMMARY
          echo "| SHA Tag  | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Platform | linux/arm64 (Graviton) |" >> $GITHUB_STEP_SUMMARY
          echo "| Registry | GHCR |" >> $GITHUB_STEP_SUMMARY

  # ─────────────────────────────────────────────────────────────────────────────
  # Stage 5a — Apply k8s config (ONLY when k8s/** changes)
  # ─────────────────────────────────────────────────────────────────────────────
  deploy-k3s-config:
    name: Apply k8s Config
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.k8s == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy k8s manifests to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ vars.SERVER_HOST }}
          username: ${{ vars.SERVER_USER }}
          key: ${{ vars.SSH_PRIVATE_KEY }}
          source: "k8s"
          target: "~/k8s-deploy/"

      - name: Apply manifests
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ vars.SERVER_HOST }}
          username: ${{ vars.SERVER_USER }}
          key: ${{ vars.SSH_PRIVATE_KEY }}
          script: |
            sudo k3s kubectl create namespace edgebalancer --dry-run=client -o yaml | sudo k3s kubectl apply -f -
            sudo k3s kubectl apply -f ~/k8s-deploy/k8s/
            rm -rf ~/k8s-deploy
            echo "k8s config applied successfully!"

      - name: Annotate summary
        run: |
          echo "## k8s Config Applied" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Detail   | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Manifests | Deployment, HPA, Service, Ingress, Redis |" >> $GITHUB_STEP_SUMMARY
          echo "| Host     | \`${{ vars.SERVER_HOST }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Status   | applied |" >> $GITHUB_STEP_SUMMARY

#   # ─────────────────────────────────────────────────────────────────────────────
#   # Stage 5b — Rolling image update (ONLY when server/** changes)
#   # ─────────────────────────────────────────────────────────────────────────────
#   deploy-k3s-image:
#     name: Rolling Update Image
#     runs-on: ubuntu-latest
#     needs: [changes, build-push-server, deploy-k3s-config]
#     if: |
#       always() &&
#       needs.changes.outputs.server == 'true' &&
#       needs.build-push-server.result == 'success' &&
#       (needs.deploy-k3s-config.result == 'success' || needs.deploy-k3s-config.result == 'skipped')
#     steps:
#       - name: Lowercase owner
#         id: owner
#         run: echo "name=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT
# 
#       - name: Update image and env secrets
#         uses: appleboy/ssh-action@v1.0.3
#         env:
#           FIREBASE_PRIVATE_KEY: ${{ secrets.FIREBASE_PRIVATE_KEY }}
#           CF_ORIGIN_CERT: ${{ secrets.CF_ORIGIN_CERT }}
#           CF_ORIGIN_KEY: ${{ secrets.CF_ORIGIN_KEY }}
#         with:
#           host: ${{ vars.SERVER_HOST }}
#           username: ${{ vars.SERVER_USER }}
#           key: ${{ vars.SSH_PRIVATE_KEY }}
#           envs: FIREBASE_PRIVATE_KEY,CF_ORIGIN_CERT,CF_ORIGIN_KEY
#           script: |
#             IMAGE="${{ needs.build-push-server.outputs.image }}"
# 
#             sudo k3s kubectl create namespace edgebalancer --dry-run=client -o yaml | sudo k3s kubectl apply -f -
# 
#             echo "Upserting Cloudflare Origin TLS secret..."
#             printf '%s' "$CF_ORIGIN_CERT" > /tmp/tls.crt
#             printf '%s' "$CF_ORIGIN_KEY"  > /tmp/tls.key
#             sudo k3s kubectl create secret tls edgebalancer-tls \
#               --namespace edgebalancer \
#               --cert=/tmp/tls.crt \
#               --key=/tmp/tls.key \
#               --dry-run=client -o yaml | sudo k3s kubectl apply -f -
#             rm -f /tmp/tls.crt /tmp/tls.key
# 
#             echo "Upserting GHCR pull secret..."
#             sudo k3s kubectl create secret docker-registry ghcr-pull-secret \
#               --namespace edgebalancer \
#               --docker-server=ghcr.io \
#               --docker-username=${{ steps.owner.outputs.name }} \
#               --docker-password=${{ secrets.GHCR_TOKEN }} \
#               --dry-run=client -o yaml | sudo k3s kubectl apply -f -
# 
#             echo "Upserting app env secret..."
#             FIREBASE_KEY=$(printf '%s' "$FIREBASE_PRIVATE_KEY" | sed 's/\\n/\n/g')
#             sudo k3s kubectl create secret generic edgebalancer-env \
#               --namespace edgebalancer \
#               --from-literal=MONGODB_URI="${{ secrets.MONGODB_URI }}" \
#               --from-literal=JWT_SECRET="${{ secrets.JWT_SECRET }}" \
#               --from-literal=ENCRYPTION_KEY="${{ secrets.ENCRYPTION_KEY }}" \
#               --from-literal=CLIENT_URL="${{ secrets.CLIENT_URL }}" \
#               --from-literal=CORS_ORIGIN="${{ secrets.CORS_ORIGIN }}" \
#               --from-literal=FIREBASE_PROJECT_ID="${{ secrets.FIREBASE_PROJECT_ID }}" \
#               --from-literal=FIREBASE_CLIENT_EMAIL="${{ secrets.FIREBASE_CLIENT_EMAIL }}" \
#               --from-literal=FIREBASE_PRIVATE_KEY="$FIREBASE_KEY" \
#               --from-literal=MISTRAL_API_KEY="${{ secrets.MISTRAL_API_KEY }}" \
#               --from-literal=OPENROUTER_API_KEY="${{ secrets.OPENROUTER_API_KEY }}" \
#               --from-literal=CLOUDFLARE_OAUTH_CLIENT_ID="${{ secrets.CLOUDFLARE_OAUTH_CLIENT_ID }}" \
#               --from-literal=CLOUDFLARE_OAUTH_CLIENT_SECRET="${{ secrets.CLOUDFLARE_OAUTH_CLIENT_SECRET }}" \
#               --from-literal=CLOUDFLARE_OAUTH_REDIRECT_URI="${{ secrets.CLOUDFLARE_OAUTH_REDIRECT_URI }}" \
#               --from-literal=CASHFREE_APP_ID="${{ secrets.CASHFREE_APP_ID }}" \
#               --from-literal=CASHFREE_SECRET_KEY="${{ secrets.CASHFREE_SECRET_KEY }}" \
#               --from-literal=CASHFREE_ENV="${{ secrets.CASHFREE_ENV }}" \
#               --dry-run=client -o yaml | sudo k3s kubectl apply -f -
# 
#             echo "Rolling update to: $IMAGE"
#             sudo k3s kubectl set image deployment/edgebalancer-server \
#               edgebalancer-server=$IMAGE \
#               --namespace edgebalancer
# 
#             echo "Waiting for rollout..."
#             if ! sudo k3s kubectl rollout status deployment/edgebalancer-server \
#               --namespace edgebalancer --timeout=5m; then
#               echo "Rollout failed — rolling back to previous version..."
#               sudo k3s kubectl rollout undo deployment/edgebalancer-server \
#                 --namespace edgebalancer
#               sudo k3s kubectl rollout status deployment/edgebalancer-server \
#                 --namespace edgebalancer --timeout=3m
#               echo "Rollback complete."
#               exit 1
#             fi
# 
#             echo "Deployed $IMAGE successfully!"
# 
#       - name: Annotate summary
#         run: |
#           VERSION="${{ needs.build-push-server.outputs.version }}"
#           IMAGE="${{ needs.build-push-server.outputs.image }}"
#           echo "## Deployed to k3s" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "| Detail    | Value |" >> $GITHUB_STEP_SUMMARY
#           echo "|-----------|-------|" >> $GITHUB_STEP_SUMMARY
#           echo "| Version   | \`v$VERSION\` |" >> $GITHUB_STEP_SUMMARY
#           echo "| Image     | \`$IMAGE\` |" >> $GITHUB_STEP_SUMMARY
#           echo "| Host      | \`${{ vars.SERVER_HOST }}\` |" >> $GITHUB_STEP_SUMMARY
#           echo "| Strategy  | Rolling Update (maxSurge=1, maxUnavailable=0) |" >> $GITHUB_STEP_SUMMARY
#           echo "| Replicas  | HPA 1–2 @ 70% CPU |" >> $GITHUB_STEP_SUMMARY
#           echo "| Resources | requests 200m/128Mi · limits 500m/256Mi |" >> $GITHUB_STEP_SUMMARY
#           echo "| Status    | deployed |" >> $GITHUB_STEP_SUMMARY
# 
  # ─────────────────────────────────────────────────────────────────────────────
  # Stage 5c — Deploy to AWS ECS via Terraform (replaces k3s SSH deploy)
  # ─────────────────────────────────────────────────────────────────────────────
  deploy-aws-terraform:
    name: Deploy to AWS ECS
    runs-on: ubuntu-latest
    needs: [changes, build-push-server]
    if: |
      always() &&
      (needs.changes.outputs.server == 'true' || needs.changes.outputs.aws == 'true') &&
      needs.build-push-server.result != 'failure' &&
      needs.build-push-server.result != 'cancelled'
    env:
      # AWS credentials from GitHub Variables (as requested)
      AWS_ACCESS_KEY_ID: ${{ vars.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ vars.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: ${{ vars.AWS_REGION }}
      # Terraform variables passed as env (auto-masked by GitHub Actions)
      TF_VAR_aws_region: ${{ vars.AWS_REGION }}
      TF_VAR_docker_image: ${{ needs.build-push-server.outputs.image || format('ghcr.io/{0}/edgebalancer-server:latest', github.repository_owner) }}
      TF_VAR_jwt_secret: ${{ secrets.JWT_SECRET }}
      TF_VAR_encryption_key: ${{ secrets.ENCRYPTION_KEY }}
      TF_VAR_mongodb_uri: ${{ secrets.MONGODB_URI }}
      TF_VAR_client_url: ${{ secrets.CLIENT_URL }}
      TF_VAR_cors_origin: ${{ secrets.CORS_ORIGIN }}
      TF_VAR_cf_origin_cert: ${{ secrets.CF_ORIGIN_CERT }}
      TF_VAR_cf_origin_key: ${{ secrets.CF_ORIGIN_KEY }}
      TF_VAR_firebase_project_id: ${{ secrets.FIREBASE_PROJECT_ID }}
      TF_VAR_firebase_client_email: ${{ secrets.FIREBASE_CLIENT_EMAIL }}
      TF_VAR_firebase_private_key: ${{ secrets.FIREBASE_PRIVATE_KEY }}
      TF_VAR_mistral_api_key: ${{ secrets.MISTRAL_API_KEY }}
      TF_VAR_openrouter_api_key: ${{ secrets.OPENROUTER_API_KEY }}
      TF_VAR_cloudflare_oauth_client_id: ${{ secrets.CLOUDFLARE_OAUTH_CLIENT_ID }}
      TF_VAR_cloudflare_oauth_client_secret: ${{ secrets.CLOUDFLARE_OAUTH_CLIENT_SECRET }}
      TF_VAR_cloudflare_oauth_redirect_uri: ${{ secrets.CLOUDFLARE_OAUTH_REDIRECT_URI }}
      TF_VAR_cashfree_app_id: ${{ secrets.CASHFREE_APP_ID }}
      TF_VAR_cashfree_secret_key: ${{ secrets.CASHFREE_SECRET_KEY }}
      TF_VAR_cashfree_env: ${{ secrets.CASHFREE_ENV }}
    steps:
      - name: Mask AWS credentials
        run: |
          echo "::add-mask::${{ vars.AWS_ACCESS_KEY_ID }}"
          echo "::add-mask::${{ vars.AWS_SECRET_ACCESS_KEY }}"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.6+"

      - name: Terraform Init
        working-directory: aws
        run: terraform init -input=false

      - name: Terraform Validate
        working-directory: aws
        run: terraform validate

      - name: Terraform Apply
        working-directory: aws
        run: terraform apply -auto-approve
        env:
          TF_LOG: INFO

      - name: Deploy new container
        working-directory: aws
        run: |
          CLUSTER=$(terraform output -raw ecs_cluster_name)
          SERVICE=$(terraform output -raw ecs_service_name)
          aws ecs update-service \
            --cluster "$CLUSTER" \
            --service "$SERVICE" \
            --force-new-deployment

      - name: Annotate summary
        run: |
          IMAGE="${{ needs.build-push-server.outputs.image }}"
          VERSION="${{ needs.build-push-server.outputs.version }}"
          echo "## Deployed to AWS ECS" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Detail  | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Version | \`v$VERSION\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Image   | \`$IMAGE\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Region  | \`${{ vars.AWS_REGION }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Target  | ECS on Fargate |" >> $GITHUB_STEP_SUMMARY
          echo "| Status  | deployed |" >> $GITHUB_STEP_SUMMARY
